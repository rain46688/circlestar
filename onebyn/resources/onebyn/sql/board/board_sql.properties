getboardlist=SELECT * FROM (SELECT ROWNUM NUM, N.* FROM (SELECT * FROM BOARD ORDER BY ENROLL_DATE DESC) N) WHERE NUM BETWEEN ? AND ?
getboardcnt=SELECT COUNT(BOARD_ID) COUNT FROM (SELECT ROWNUM NUM, N.* FROM (SELECT * FROM BOARD ORDER BY ENROLL_DATE DESC) N)
writeboardlist=INSERT INTO BOARD(BOARD_TITLE,WRITER_ID,CONTENT,FILES,PRODUCT_CATEGORI,PRODUCT_NAME,TRADE_AREA) VALUES(?,?,?,?,'특가','오! 그래놀라','강동구 상일동')